name: CI

on:
  push:
    branches: [main, develop, "feature/**"]
  pull_request:
    branches: [main, develop]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'
      
      - name: Install .NET MAUI Android Workload
        run: dotnet workload install maui-android

      - name: Restore dependencies
        run: dotnet restore Briefcase.slnx

      - name: Build
        run: dotnet build Briefcase.slnx --no-restore --configuration Release

      - name: Run unit tests
        run: dotnet test tests/Briefcase.UnitTests/Briefcase.UnitTests.csproj --no-build --configuration Release --logger "trx;LogFileName=test-results.trx"

      - name: Run integration tests
        run: dotnet test tests/Briefcase.IntegrationTests/Briefcase.IntegrationTests.csproj --no-build --configuration Release --logger "trx;LogFileName=integration-test-results.trx"

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results
          path: "**/*test-results.trx"

      - name: Publish test results
        if: always()
        uses: dorny/test-reporter@v3
        with:
          name: Test Results
          path: "**/*test-results.trx"
          reporter: dotnet-trx
